# Optional. If set, the UI will call this base URL directly instead of the
# Vite dev proxy. Useful for production builds pointing at a deployed API.
# Example:
# VITE_API_BASE_URL=http://localhost:8081/api/v1
